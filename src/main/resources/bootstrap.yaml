spring:
  application:
    name: reload-example
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: event
        strategy: refresh
      config:
        sources:
          - name: reload-example


